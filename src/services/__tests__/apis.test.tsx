import { describe, it, expect } from 'vitest';
import { api } from '../apis';

describe('apis', () => {
    it('should have the correct API endpoints', () => {
        // expect(api.myorders.get).toBe('myorders/get');
        // Add assertions for other API endpoints
    });
});
